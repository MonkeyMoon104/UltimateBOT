/**
 * Public bot event contracts, grouped into domain-specific subpackages.
 *
 * <p>Use {@code base} for shared metadata, {@code bus} for subscriptions,
 * {@code lifecycle} for bot lifetime changes, {@code combat} for combat activity,
 * {@code state} for configuration and target changes, and {@code action} for
 * independent bot actions.</p>
 */
@org.jspecify.annotations.NullMarked
package com.monkey.mcbot.api.event;
