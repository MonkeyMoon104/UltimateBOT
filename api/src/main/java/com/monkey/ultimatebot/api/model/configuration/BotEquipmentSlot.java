package com.monkey.ultimatebot.api.model.configuration;

/** Equipment positions that can be fixed for a managed bot. */
public enum BotEquipmentSlot {
    MAIN_HAND,
    OFF_HAND,
    HEAD,
    CHEST,
    LEGS,
    FEET
}
